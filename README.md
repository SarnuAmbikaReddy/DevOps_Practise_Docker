# DevOps_Practise_Docker
This Repo help create docker image and push it to a registry

## Install Docker
1. Installin Docker on Linux machine
2. execute below commands
   a. sudo apt update                #Update all the packages
   b. sudo apt install docker.io     #Installing Docker
   c. sudo systemctl status docker   #Verifying the status of Docker

## Download any image
